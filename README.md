# Notebook Work Library

This repository contains the static website for Notebook Work Library (class materials). Deploy this repository to GitHub Pages (branch `main`) to host the site.

How to publish:

1. Create a repository on GitHub and push this project to it.
2. Enable GitHub Pages from `main` branch (or use the provided GitHub Actions workflow to deploy automatically).
